import { StyleSheet } from 'aphrodisiac';

export default StyleSheet.create({
  active: {
    color: '#288fb2',
  },
  default: {
    cursor: 'default',
  },
  disabled: {
    cursor: 'not-allowed',
  },
  pointer: {
    cursor: 'pointer',
  },
});
