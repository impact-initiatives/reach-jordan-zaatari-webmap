general:
  branches:
    only:
      - master
      - staging
      - develop
test:
  override:
    - npm run lint
dependencies:
  pre:
    - pip install awscli
    - cp .jspm.config.js jspm.config.js
  post:
    - jspm install
    - npm run build
deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh master
  staging:
    branch: staging
    commands:
      - ./deploy.sh staging
  dev:
    branch: develop
    commands:
      - ./deploy.sh develop
